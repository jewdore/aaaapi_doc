---
title: 'sora-2 (Text to Video)'
sidebarTitle: 'Text to Video'
api: "POST /v1/chat/completions"
description: "Generate videos from text descriptions using OpenAI's Sora model"
mode: 'wide'
---

import Authentication from '/snippets/common/authentication.mdx';

<Authentication/>

## Initiate Request
<CodeGroup>

```bash cURL
curl --location --request POST 'https://gptproto.com/v1/chat/completions' \
--header 'Authorization: GPTPROTO_API_KEY' \
--header 'Content-Type: application/json' \
--data-raw '{
  "model": "sora-2",
  "messages": [
    {
      "role": "user",
      "content": [
        {
          "type": "text",
          "text": "Put a hat on the cat"
        }
      ]
    }
  ]
}'
```

```python Python
from openai import OpenAI

client = OpenAI(
    api_key="GPTPROTO_API_KEY",
    base_url="https://gptproto.com/v1"
)

response = client.chat.completions.create(
    model="sora-2",
    messages=[
        {
            "role": "user",
            "content": [
                {
                    "type": "text",
                    "text": "Put a hat on the cat"
                }
            ]
        }
    ]
)

print(response)
```

```javascript JavaScript
const OpenAI = require('openai');

const openai = new OpenAI({
    apiKey: 'GPTPROTO_API_KEY',
    baseURL: 'https://gptproto.com/v1'
});

async function main() {
    const response = await openai.chat.completions.create({
        model: 'sora-2',
        messages: [
            {
                role: 'user',
                content: [
                    {
                        type: 'text',
                        text: 'Put a hat on the cat'
                    }
                ]
            }
        ]
    });

    console.log(response);
}

main();
```

```go Go
package main

import (
    "context"
    "fmt"
    "github.com/sashabaranov/go-openai"
)

func main() {
    config := openai.DefaultConfig("GPTPROTO_API_KEY")
    config.BaseURL = "https://gptproto.com/v1"
    client := openai.NewClientWithConfig(config)

    resp, err := client.CreateChatCompletion(
        context.Background(),
        openai.ChatCompletionRequest{
            Model: "sora-2",
            Messages: []openai.ChatCompletionMessage{
                {
                    Role: openai.ChatMessageRoleUser,
                    MultiContent: []openai.ChatMessagePart{
                        {
                            Type: openai.ChatMessagePartTypeText,
                            Text: "Put a hat on the cat",
                        },
                    },
                },
            },
        },
    )

    if err != nil {
        fmt.Printf("ChatCompletion error: %v\n", err)
        return
    }

    fmt.Println(resp)
}
```
</CodeGroup>

## Response Example
```json
{
    "choices": [
        {
            "finish_reason": "stop",
            "index": 0,
            "message": {
                "content": "[download video](https://gptproto-us.oss-us-east-1.aliyuncs.com/sora/123.mp4)\n\n",
                "role": "assistant"
            }
        }
    ],
    "created": 1766560185,
    "id": "foaicmpl-123",
    "model": "sora-2",
    "object": "chat.completion",
    "usage": {
        "completion_tokens": 49,
        "prompt_tokens": 15,
        "total_tokens": 64
    }
}
```

import Parameters from '/snippets/parameters/openai-format/common-chat-format.mdx';

<Parameters/>

import ErrorCode from '/snippets/common/errors_code.mdx';

<ErrorCode/>