---
title: 'sora-2-pro (Image to Video)'
sidebarTitle: 'Image to Video'
api: "POST /v1/videos"
description: "Generate videos from input images using OpenAI Sora 2 Pro model"
mode: 'wide'
---

import Authentication from '/snippets/common/authentication.mdx';

<Authentication/>

## Initiate Request
<CodeGroup>

```bash cURL
curl --location --request POST 'https://gptproto.com/v1/videos' \
--header 'Authorization: GPTPROTO_API_KEY' \
--form 'model=sora-2-pro' \
--form 'prompt=Put a hat on the cat' \
--form 'input_reference=@cat.jpg'
```

```python Python
import requests

url = "https://gptproto.com/v1/videos"
headers = {
    "Authorization": "GPTPROTO_API_KEY"
}
files = {
    "input_reference": open("cat.jpg", "rb")
}
data = {
    "model": "sora-2-pro",
    "prompt": "Put a hat on the cat"
}

response = requests.post(url, headers=headers, files=files, data=data)
print(response.json())
```

```javascript JavaScript
const FormData = require('form-data');
const fs = require('fs');
const axios = require('axios');

const form = new FormData();
form.append('model', 'sora-2-pro');
form.append('prompt', 'Put a hat on the cat');
form.append('input_reference', fs.createReadStream('cat.jpg'));

axios.post('https://gptproto.com/v1/videos', form, {
  headers: {
    'Authorization': 'GPTPROTO_API_KEY',
    ...form.getHeaders()
  }
})
.then(response => console.log(response.data))
.catch(error => console.error(error));
```

```go Go
package main

import (
    "bytes"
    "fmt"
    "io"
    "mime/multipart"
    "net/http"
    "os"
)

func main() {
    url := "https://gptproto.com/v1/videos"
    
    body := &bytes.Buffer{}
    writer := multipart.NewWriter(body)
    
    writer.WriteField("model", "sora-2-pro")
    writer.WriteField("prompt", "Put a hat on the cat")
    
    file, _ := os.Open("cat.jpg")
    defer file.Close()
    part, _ := writer.CreateFormFile("input_reference", "cat.jpg")
    io.Copy(part, file)
    
    writer.Close()
    
    req, _ := http.NewRequest("POST", url, body)
    req.Header.Set("Authorization", "GPTPROTO_API_KEY")
    req.Header.Set("Content-Type", writer.FormDataContentType())
    
    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()
    
    responseBody, _ := io.ReadAll(resp.Body)
    fmt.Println(string(responseBody))
}
```

</CodeGroup>

## Response Example
```json
{
  "id": "video_123",
  "object": "video",
  "model": "sora-2-pro",
  "status": "queued",
  "progress": 0,
  "created_at": 1712697600,
  "size": "1024x1792",
  "seconds": "8",
  "quality": "standard"
}
```

import QueryResult from '/snippets/parameters/official-format/OpenAI/sora-2-query.mdx';

<QueryResult/>

import Parameters from '/snippets/parameters/official-format/OpenAI/sora-2.mdx';

<Parameters/>

import ErrorCode from '/snippets/common/errors_code.mdx';

<ErrorCode/>
