---
title: 'gpt-image-1.5-plus (Image Edit)'
sidebarTitle: 'Image Edit'
api: "POST /api/v3/openai/gpt-image-1.5-plus/image-edit"
description: "Edit and modify images using OpenAI's GPT Image 1.5 Plus model"
mode: 'wide'
---

import Authentication from '/snippets/common/authentication.mdx';

<Authentication/>

## Initiate Request
<CodeGroup>

```bash cURL
curl --location --request POST "https://gptproto.com/api/v3/openai/gpt-image-1.5-plus/image-edit" \
--header "Authorization: GPTPROTO_API_KEY" \
--header "Content-Type: application/json" \
--data-raw '{
    "image": [
        "https://tos.gptproto.com/resource/cat.png"
    ],
    "prompt": "Girl holding cat",
    "quality": "medium",
    "size": "1024x1024",
    "background": "auto",
    "enable_sync_mode": false,
    "response_format": "url"
}'
```

```python Python
import requests
import json

url = "https://gptproto.com/api/v3/openai/gpt-image-1.5-plus/image-edit"

headers = {
    "Authorization": "GPTPROTO_API_KEY",
    "Content-Type": "application/json"
}

payload = {
    "image": [
        "https://tos.gptproto.com/resource/cat.png"
    ],
    "prompt": "Girl holding cat",
    "quality": "medium",
    "size": "1024x1024",
    "background": "auto",
    "enable_sync_mode": False,
    "response_format": "url"
}

response = requests.post(url, headers=headers, data=json.dumps(payload))
print(response.json())
```

```javascript JavaScript
const axios = require('axios');

const url = 'https://gptproto.com/api/v3/openai/gpt-image-1.5-plus/image-edit';

const headers = {
    'Authorization': 'GPTPROTO_API_KEY',
    'Content-Type': 'application/json'
};

const data = {
    image: [
        'https://tos.gptproto.com/resource/cat.png'
    ],
    prompt: 'Girl holding cat',
    quality: 'medium',
    size: '1024x1024',
    background: 'auto',
    enable_sync_mode: false,
    response_format: 'url'
};

axios.post(url, data, { headers })
    .then(response => console.log(response.data))
    .catch(error => console.error(error));
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io/ioutil"
    "net/http"
)

func main() {
    url := "https://gptproto.com/api/v3/openai/gpt-image-1.5-plus/image-edit"

    payload := map[string]interface{}{
        "image": []string{
            "https://tos.gptproto.com/resource/cat.png",
        },
        "prompt":           "Girl holding cat",
        "quality":          "medium",
        "size":             "1024x1024",
        "background":       "auto",
        "enable_sync_mode": false,
        "response_format":  "url",
    }

    jsonData, _ := json.Marshal(payload)

    req, _ := http.NewRequest("POST", url, bytes.NewBuffer(jsonData))
    req.Header.Set("Authorization", "GPTPROTO_API_KEY")
    req.Header.Set("Content-Type", "application/json")

    client := &http.Client{}
    resp, err := client.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()

    body, _ := ioutil.ReadAll(resp.Body)
    fmt.Println(string(body))
}
```

</CodeGroup>

## Response Example
```json
{
    "data": {
        "id": "7ba2419*******",
        "model": "gpt-image-1.5-plus",
        "outputs": [],
        "urls": {
            "get": "https://gptproto.com/api/v3/predictions/7ba2419*******/result"
        },
        "hasNsfwContents": [],
        "status": "created",
        "createdAt": "2025-12-17 15:05:05",
        "executionTime": 0,
        "timings": {
            "inference": 0
        }
    },
    "message": "success",
    "code": 200
}
```

## Query Result

import QueryResult from '/snippets/parameters/gptproto-format/common/query-result.mdx';

<QueryResult/>

## Parameters

import Parameters from '/snippets/parameters/gptproto-format/OpenAI/imagemodel.mdx';

<Parameters/>

## Error Codes

import ErrorCode from '/snippets/common/errors_code.mdx';

<ErrorCode/>