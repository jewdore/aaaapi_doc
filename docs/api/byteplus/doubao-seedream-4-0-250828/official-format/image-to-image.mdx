---
title: 'doubao-seedream-4-0-250828 (image to image)'
sidebarTitle: 'Image to Image'
api: 'POST /api/v3/chat/completions'
description: 'Image to Image'
mode: 'wide'
---

BytePlus's official format for the image to image API.









<CodeGroup>

```bash cURL
curl -X POST "https://gptproto.com/api/v3/chat/completions" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "model": "doubao-seedream-4-0-250828",
  "messages": [
    {
      "role": "user",
      "content": "Hello, please introduce yourself"
    }
  ],
  "temperature": 0.7,
  "max_tokens": 2048
}'
```

```python Python
import requests
import json

url = "https://gptproto.com/api/v3/chat/completions"
headers = {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
}

data = {
    "model": "doubao-seedream-4-0-250828",
    "messages": [
        {"role": "user", "content": "Hello, please introduce yourself"}
    ],
    "temperature": 0.7,
    "max_tokens": 2048
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(json.dumps(result, indent=2, ensure_ascii=False))
```

```javascript JavaScript
const url = "https://gptproto.com/api/v3/chat/completions";
const headers = {
  "Authorization": "Bearer YOUR_API_KEY",
  "Content-Type": "application/json"
};

const data = {
  model: "doubao-seedream-4-0-250828",
  messages: [
    { role: "user", content: "Hello, please introduce yourself" }
  ],
  temperature: 0.7,
  max_tokens: 2048
};

fetch(url, {
  method: "POST",
  headers: headers,
  body: JSON.stringify(data)
})
  .then(response => response.json())
  .then(data => console.log(data))
  .catch(error => console.error("Error:", error));
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io/ioutil"
    "net/http"
)

func main() {
    url := "https://gptproto.com/api/v3/chat/completions"

    payload := []byte(`{
  "model": "doubao-seedream-4-0-250828",
  "messages": [
    {
      "role": "user",
      "content": "Hello, please introduce yourself"
    }
  ],
  "temperature": 0.7,
  "max_tokens": 2048
}`)

    req, _ := http.NewRequest("POST", url, bytes.NewBuffer(payload))
    req.Header.Set("Authorization", "Bearer YOUR_API_KEY")
    req.Header.Set("Content-Type", "application/json")

    client := &http.Client{}
    resp, err := client.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()

    body, _ := ioutil.ReadAll(resp.Body)
    fmt.Println(string(body))
}
```
</CodeGroup>

<CodeGroup>

```json 400 - Bad Request
{
  "error": {
    "message": "Invalid request parameters",
    "type": "invalid_request_error",
    "code": "invalid_parameters"
  }
}
```

```json 401 - Unauthorized
{
  "error": {
    "message": "Invalid authentication credentials",
    "type": "authentication_error",
    "code": "invalid_api_key"
  }
}
```

```json 403 - Forbidden
{
  "error": {
    "message": "Insufficient permissions or quota exceeded",
    "type": "permission_error",
    "code": "insufficient_quota"
  }
}
```

```json 429 - Rate Limit Exceeded
{
  "error": {
    "message": "Rate limit exceeded",
    "type": "rate_limit_error",
    "code": "rate_limit_exceeded"
  }
}
```

```json 500 - Internal Server Error
{
  "error": {
    "message": "Internal server error",
    "type": "server_error",
    "code": "internal_error"
  }
}
```
</CodeGroup>

## Parameters

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `model` | string | ✅ Yes | `doubao-seedream-4-0-250828` | The endpoint ID for the model. Format: ep-xxxxxxxxxx-xxxxx |
| `messages` | array | ✅ Yes | `[{'role': 'user', 'content': 'Who are you?'}]` | Array of message objects for the conversation. Each message must have a role (system, user, or assistant) and content. |
| `temperature` | number | ❌ No | `0.7` | Controls randomness in the output. Range: 0.0 to 1.0 |
| `top_p` | number | ❌ No | `0.9` | Nucleus sampling parameter. Range: 0.0 to 1.0 |
| `max_tokens` | integer | ❌ No | `2048` | The maximum number of tokens to generate |
| `stream` | boolean | ❌ No | `false` | Whether to stream the response using SSE (Server-Sent Events) |
| `frequency_penalty` | number | ❌ No | `0.0` | Penalize frequent tokens. Range: -2.0 to 2.0 |
| `presence_penalty` | number | ❌ No | `0.0` | Penalize new tokens based on presence. Range: -2.0 to 2.0 |
| `stop` | array | ❌ No | - | Up to 4 sequences where the API will stop generating further tokens |










