---
title: 'Query Status'
api: 'GET /v1/suno/fetch/{task_id}'
description: 'Check the status and retrieve generated music from Suno AI tasks. Monitor asynchronous music generation progress and download completed audio files.'
mode: 'wide'
---

## Overview

Suno's query task.mdx for the query task API.









<CodeGroup>

```bash cURL
curl --location 'https://gptproto.com/v1/suno/fetch/15410652-357c-47fa-8ad9-a4a145fbfa14' \
--header 'Authorization: YOUR_API_KEY' \
--header 'Content-Type: application/json'
```

```python Python
import requests

task_id = "15410652-357c-47fa-8ad9-a4a145fbfa14"
url = f"https://gptproto.com/v1/suno/fetch/{task_id}"

headers = {
    "Authorization": "YOUR_API_KEY",
    "Content-Type": "application/json"
}

response = requests.get(url, headers=headers)
result = response.json()

if result["code"] == "success" and result["data"]:
    task = result["data"][0]
    print(f"Status: {task['status']}")

    if task['status'] == 'completed':
        print(f"Audio URL: {task['audio_url']}")
```

```javascript JavaScript
const taskId = "15410652-357c-47fa-8ad9-a4a145fbfa14";
const url = `https://gptproto.com/v1/suno/fetch/${taskId}`;

const headers = {
  "Authorization": "YOUR_API_KEY",
  "Content-Type": "application/json"
};

fetch(url, {
  method: "GET",
  headers: headers
})
  .then(response => response.json())
  .then(result => {
    if (result.code === "success" && result.data) {
      const task = result.data[0];
      console.log(`Status: ${task.status}`);

      if (task.status === "completed") {
        console.log(`Audio URL: ${task.audio_url}`);
      }
    }
  })
  .catch(error => console.error("Error:", error));
```

```go Go
package main

import (
    "encoding/json"
    "fmt"
    "io/ioutil"
    "net/http"
)

type TaskData struct {
    ID       string `json:"id"`
    Status   string `json:"status"`
    AudioURL string `json:"audio_url,omitempty"`
    VideoURL string `json:"video_url,omitempty"`
    Title    string `json:"title,omitempty"`
}

type Response struct {
    Code    string      `json:"code"`
    Data    []TaskData  `json:"data"`
    Message string      `json:"message"`
}

func main() {
    taskID := "15410652-357c-47fa-8ad9-a4a145fbfa14"
    url := fmt.Sprintf("https://gptproto.com/v1/suno/fetch/%s", taskID)

    req, _ := http.NewRequest("GET", url, nil)
    req.Header.Set("Authorization", "YOUR_API_KEY")
    req.Header.Set("Content-Type", "application/json")

    client := &http.Client{}
    resp, err := client.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()

    body, _ := ioutil.ReadAll(resp.Body)

    var result Response
    json.Unmarshal(body, &result)

    if result.Code == "success" && len(result.Data) > 0 {
        task := result.Data[0]
        fmt.Printf("Status: %s\n", task.Status)

        if task.Status == "completed" {
            fmt.Printf("Audio URL: %s\n", task.AudioURL)
        }
    }
}
```
</CodeGroup>

<CodeGroup>

```json 404 - Task not found
{
  "code": "error",
  "data": null,
  "message": "Task not found"
}
```

```json 401 - Invalid API key
{
  "code": "error",
  "data": null,
  "message": "Invalid API key"
}
```

```json 403 - Insufficient balance
{
  "code": "error",
  "data": null,
  "message": "Insufficient balance"
}
```

```json 429 - Rate limit exceeded
{
  "code": "error",
  "data": null,
  "message": "Rate limit exceeded. Please try again later."
}
```

```json 500 - Internal server error
{
  "code": "error",
  "data": null,
  "message": "Internal server error"
}
```
</CodeGroup>

## Path Parameters







## Task Status Values

| Status | Description | Next Action |
|--------|-------------|-------------|
| `queued` | Task is waiting in queue to be processed | Continue polling |
| `processing` | Task is currently being processed | Continue polling, check progress |
| `completed` | Task completed successfully, audio is ready | Download audio from `audio_url` |
| `failed` | Task failed | Check `error_message`, retry if needed |



