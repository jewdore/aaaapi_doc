---
title: 'Submit Video Generation Task'
api: 'POST /runwayml/v1/image_to_video'
description: 'Submit Video Generation Task'
---

## Overview

This endpoint provides submit video generation task functionality.

## Authentication

This endpoint requires authentication using a Bearer token.

<ParamField header="Authorization" type="string" required>
  Your API key in the format: `Bearer YOUR_API_KEY`
</ParamField>

## Request Body

<ParamField body="promptImage" type="string" >
  Promptimage parameter
</ParamField>

<ParamField body="seed" type="integer" >
  Seed parameter
</ParamField>

<ParamField body="model" type="string" required>
  The model to use for the request
</ParamField>

<ParamField body="promptText" type="string" >
  Prompttext parameter
</ParamField>

<ParamField body="watermark" type="boolean" >
  Watermark parameter
</ParamField>

<ParamField body="duration" type="integer" >
  Duration parameter
</ParamField>

<ParamField body="ratio" type="string" >
  Ratio parameter
</ParamField>

## Request Example

<CodeGroup>

```bash cURL
curl -X POST "https://gptproto.com/runwayml/v1/image_to_video" \
  -H "Authorization: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "promptImage": "https://xxx.com/logo.png",
  "seed": 4294967295,
  "model": "runwayml-gen3a_turbo-5",
  "promptText": "cat dance",
  "watermark": false,
  "duration": 5,
  "ratio": "1280:768"
}'
```

```python Python
import requests
import json

url = "https://gptproto.com/runwayml/v1/image_to_video"
headers = {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
}

data = {
  "promptImage": "https://xxx.com/logo.png",
  "seed": 4294967295,
  "model": "runwayml-gen3a_turbo-5",
  "promptText": "cat dance",
  "watermark": false,
  "duration": 5,
  "ratio": "1280:768"
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(json.dumps(result, indent=2))
```

```javascript JavaScript
const url = "https://gptproto.com/runwayml/v1/image_to_video";
const headers = {
  "Authorization": "Bearer YOUR_API_KEY",
  "Content-Type": "application/json"
};

const data = {
  "promptImage": "https://xxx.com/logo.png",
  "seed": 4294967295,
  "model": "runwayml-gen3a_turbo-5",
  "promptText": "cat dance",
  "watermark": false,
  "duration": 5,
  "ratio": "1280:768"
};

fetch(url, {
  method: "POST",
  headers: headers,
  body: JSON.stringify(data)
})
  .then(response => response.json())
  .then(data => console.log(data))
  .catch(error => console.error("Error:", error));
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io/ioutil"
    "net/http"
)

func main() {
    url := "https://gptproto.com/runwayml/v1/image_to_video"

    payload := []byte(`{
"promptImage": "https://xxx.com/logo.png",
"seed": 4294967295,
"model": "runwayml-gen3a_turbo-5",
"promptText": "cat dance",
"watermark": false,
"duration": 5,
"ratio": "1280:768"
}`)

    req, _ := http.NewRequest("POST", url, bytes.NewBuffer(payload))
    req.Header.Set("Authorization", "Bearer YOUR_API_KEY")
    req.Header.Set("Content-Type", "application/json")

    client := &http.Client{}
    resp, err := client.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()

    body, _ := ioutil.ReadAll(resp.Body)
    fmt.Println(string(body))
}
```

</CodeGroup>

## Response

<ResponseField name="Success" type="200">
  Successful response

```json
{
  "status": "success"
}
```
</ResponseField>

## Error Responses

<ResponseExample>

```json 400 - Bad Request
{
  "error": {
    "message": "Invalid request parameters",
    "type": "invalid_request_error",
    "code": "invalid_parameters"
  }
}
```

```json 401 - Unauthorized
{
  "error": {
    "message": "Invalid API key",
    "type": "authentication_error",
    "code": "invalid_api_key"
  }
}
```

```json 429 - Rate Limit Exceeded
{
  "error": {
    "message": "Rate limit exceeded",
    "type": "rate_limit_error",
    "code": "rate_limit_exceeded"
  }
}
```

</ResponseExample>

