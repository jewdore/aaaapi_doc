---
title: 'sora-remix'
api: 'POST /v1/videos/video_68ed20580fd8819092c3e1779bab74300197d63c305fecf8/remix'
description: 'sora-remix'
mode: 'wide'
---

## sora (sora remix)

This endpoint provides sora-remix functionality.





<CodeGroup>

```bash cURL
curl -X POST "https://gptproto.com/v1/videos/video_68ed20580fd8819092c3e1779bab74300197d63c305fecf8/remix" \
  -H "Authorization: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "prompt": "Extend the scene with the cat taking a bow to the cheering audience"
}'
```

```python Python
import requests
import json

url = "https://gptproto.com/v1/videos/video_68ed20580fd8819092c3e1779bab74300197d63c305fecf8/remix"
headers = {
    "Authorization": "YOUR_API_KEY",
    "Content-Type": "application/json"
}

data = {
  "prompt": "Extend the scene with the cat taking a bow to the cheering audience"
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(json.dumps(result, indent=2))
```

```javascript JavaScript
const url = "https://gptproto.com/v1/videos/video_68ed20580fd8819092c3e1779bab74300197d63c305fecf8/remix";
const headers = {
  "Authorization": "YOUR_API_KEY",
  "Content-Type": "application/json"
};

const data = {
  "prompt": "Extend the scene with the cat taking a bow to the cheering audience"
};

fetch(url, {
  method: "POST",
  headers: headers,
  body: JSON.stringify(data)
})
  .then(response => response.json())
  .then(data => console.log(data))
  .catch(error => console.error("Error:", error));
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io/ioutil"
    "net/http"
)

func main() {
    url := "https://gptproto.com/v1/videos/video_68ed20580fd8819092c3e1779bab74300197d63c305fecf8/remix"

    payload := []byte(`{
"prompt": "Extend the scene with the cat taking a bow to the cheering audience"
}`)

    req, _ := http.NewRequest("POST", url, bytes.NewBuffer(payload))
    req.Header.Set("Authorization", "YOUR_API_KEY")
    req.Header.Set("Content-Type", "application/json")

    client := &http.Client{}
    resp, err := client.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()

    body, _ := ioutil.ReadAll(resp.Body)
    fmt.Println(string(body))
}
```
</CodeGroup>

<CodeGroup>

```json 401 - Invalid signature
{
  "error": {
    "message": "Invalid signature",
    "type": "401"
  }
}
```



```json 403 - Insufficient balance
{
  "error": {
    "message": "Insufficient balance",
    "type": "403"
  }
}
```

```json 500 - Internal server error
{
  "error": {
    "message": "Internal server error",
    "type": "500"
  }
}
```

```json 503 - Content policy violation
{
  "error": {
    "message": "Input may not meet the guidelines. Please adjust and try again.",
    "type": "503"
  }
}
```
</CodeGroup>

## Parameters

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `prompt` | string | ✅ Yes | `Extend the scene with the cat taking a bow to the cheering audience` | Prompt parameter |










