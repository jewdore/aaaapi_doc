---
title: 'Image to Image'
api: 'POST /api/v3/openai/gpt-image-1/image-edit'
description: 'Edit and transform images based on text prompts'
---

## Overview

This endpoint allows you to transform existing images based on text descriptions. You can apply transformations, add elements, or modify the image content according to your prompt.

## Authentication

This endpoint requires authentication using a Bearer token.

<ParamField header="Authorization" type="string" required default="sk-***********">
  Your API key in the format: `YOUR_API_KEY`
</ParamField>

<ParamField header="Content-Type" type="string" required default="application/json">
  Must be set to `application/json`
</ParamField>

## Request Body

<ParamField body="image" type="array" required>
  The images to edit. A maximum of 16 reference images can be uploaded.
</ParamField>

<ParamField body="prompt" type="string" required default="The cat is running on the grassland">
  A text description of the desired image(s). The maximum length is 32000 characters for gpt-image-1.
</ParamField>

<ParamField body="mask" type="string">
  An additional image whose fully transparent areas (e.g. where alpha is zero) indicate where image should be edited. If there are multiple images provided, the mask will be applied on the first image. Must be a valid PNG file, less than 4MB, and have the same dimensions as image.
</ParamField>

<ParamField body="n" type="integer" default="1">
  The number of images to generate. Must be between 1 and 10.
</ParamField>

<ParamField body="quality" type="string" default="auto">
  The quality of the image that will be generated. Options: `high`, `medium`, `low`. Only supported for gpt-image-1.
</ParamField>

<ParamField body="background" type="string" default="auto">
  Allows to set transparency for the background of the generated image(s). This parameter is only supported for gpt-image-1. Must be one of `transparent`, `opaque` or `auto` (default value). When `auto` is used, the model will automatically determine the best background for the image.
</ParamField>

<ParamField body="enable_sync_mode" type="string" default="false">
  Whether to enable synchronous mode. Options: `false`, `true`
</ParamField>

<ParamField body="response_format" type="string" default="b64_json">
  The format in which the generated images are returned. Must be one of `url` or `b64_json`.
</ParamField>

## Request Example

<CodeGroup>

```bash cURL
curl --location 'https://gptproto.com/api/v3/openai/gpt-image-1/image-edit' \
  --header 'Authorization: YOUR_API_KEY' \
  --header 'Content-Type: application/json' \
  --data '{
    "image": [
        "https://heyoo.oss-ap-southeast-1.aliyuncs.com/ai-draw/java/test/d95aeb5f-2081-4db5-9e41-7230f51b49e0.png"
    ],
    "prompt": "The cat is running on the grassland",
    "n": 1,
    "quality": "auto",
    "background": "auto",
    "enable_sync_mode": "false",
    "response_format": "b64_json"
}'
```

```python Python
import requests
import json

url = "https://gptproto.com/api/v3/openai/gpt-image-1/image-edit"
headers = {
    "Authorization": "YOUR_API_KEY",
    "Content-Type": "application/json"
}

data = {
    "image": [
        "https://heyoo.oss-ap-southeast-1.aliyuncs.com/ai-draw/java/test/d95aeb5f-2081-4db5-9e41-7230f51b49e0.png"
    ],
    "prompt": "The cat is running on the grassland",
    "n": 1,
    "quality": "auto",
    "background": "auto",
    "enable_sync_mode": "false",
    "response_format": "b64_json"
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(json.dumps(result, indent=2))
```

```javascript JavaScript
const url = "https://gptproto.com/api/v3/openai/gpt-image-1/image-edit";
const headers = {
  "Authorization": "YOUR_API_KEY",
  "Content-Type": "application/json"
};

const data = {
  "image": [
    "https://heyoo.oss-ap-southeast-1.aliyuncs.com/ai-draw/java/test/d95aeb5f-2081-4db5-9e41-7230f51b49e0.png"
  ],
  "prompt": "The cat is running on the grassland",
  "n": 1,
  "quality": "auto",
  "background": "auto",
  "enable_sync_mode": "false",
  "response_format": "b64_json"
};

fetch(url, {
  method: "POST",
  headers: headers,
  body: JSON.stringify(data)
})
  .then(response => response.json())
  .then(data => console.log(data))
  .catch(error => console.error("Error:", error));
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io/ioutil"
    "net/http"
)

func main() {
    url := "https://gptproto.com/api/v3/openai/gpt-image-1/image-edit"

    payload := []byte(`{
  "image": [
    "https://heyoo.oss-ap-southeast-1.aliyuncs.com/ai-draw/java/test/d95aeb5f-2081-4db5-9e41-7230f51b49e0.png"
  ],
  "prompt": "The cat is running on the grassland",
  "n": 1,
  "quality": "auto",
  "background": "auto",
  "enable_sync_mode": "false",
  "response_format": "b64_json"
}`)

    req, _ := http.NewRequest("POST", url, bytes.NewBuffer(payload))
    req.Header.Set("Authorization", "YOUR_API_KEY")
    req.Header.Set("Content-Type", "application/json")

    client := &http.Client{}
    resp, err := client.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()

    body, _ := ioutil.ReadAll(resp.Body)
    fmt.Println(string(body))
}
```

</CodeGroup>

## Response

<ResponseField name="Success (Async Mode)" type="200">
  Successful response when `enable_sync_mode` is `false`

```json
{
  "task_id": "abc123def456",
  "status": "processing",
  "message": "Image editing task has been submitted"
}
```
</ResponseField>

<ResponseField name="Success (Sync Mode)" type="200">
  Successful response when `enable_sync_mode` is `true`

```json
{
  "created": 1700000000,
  "data": [
    {
      "url": "https://example.com/edited-image-1.png",
      "b64_json": null
    },
    {
      "url": "https://example.com/edited-image-2.png",
      "b64_json": null
    }
  ]
}
```
</ResponseField>

## Error Responses

<ResponseExample>

```json 401 - Invalid signature
{
  "error": {
    "message": "Invalid signature",
    "type": "401"
  }
}
```

```json 403 - Invalid Token
{
  "error": {
    "message": "Invalid Token",
    "type": "403"
  }
}
```

```json 403 - Insufficient balance
{
  "error": {
    "message": "Insufficient balance",
    "type": "403"
  }
}
```

```json 400 - Invalid parameters
{
  "error": {
    "message": "Invalid image URL or parameters",
    "type": "400"
  }
}
```

```json 500 - Internal server error
{
  "error": {
    "message": "Internal server error",
    "type": "500"
  }
}
```

```json 503 - Content policy violation
{
  "error": {
    "message": "Input may not meet the guidelines. Please adjust and try again.",
    "type": "503"
  }
}
```

</ResponseExample>
