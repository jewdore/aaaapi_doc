---
title: 'Image to Image'
api: 'POST /v1/images/edits'
description: 'Edit an existing image based on a text prompt. This OpenAI-compatible endpoint is powered by Gemini 2.5 Flash Image, providing advanced image editing capabilities. [OpenAI API Reference](https://platform.openai.com/docs/api-reference/images/createEdit) | [Gemini Documentation](https://ai.google.dev/gemini-api/docs/image-generation)'
mode: 'wide'
---

## Caution

The `gemini-2.0-flash-preview-image-generation` and `gemini-2.5-flash-image-preview` models will be retired on October 31, 2025. Migrate any workflows to `gemini-2.5-flash-image` before that date to avoid service disruption.

## Overview

Gemini's openai format for the image editing API.











<CodeGroup>

```bash cURL
curl -X POST "https://gptproto.com/v1/images/edits" \
  -H "Authorization: YOUR_API_KEY" \
  -F "model=gemini-2.5-flash-image-preview" \
  -F "image=@/path/to/image.png" \
  -F "prompt=Add a wizard hat to the cat's head" \
  -F "n=1" \
  -F "size=1024x1024" \
  -F "response_format=url"
```

```python Python
import requests

url = "https://gptproto.com/v1/images/edits"
headers = {
    "Authorization": "YOUR_API_KEY"
}

files = {
    "image": open("/path/to/image.png", "rb")
}

data = {
    "model": "gemini-2.5-flash-image-preview",
    "prompt": "Add a wizard hat to the cat's head",
    "n": 1,
    "size": "1024x1024",
    "response_format": "url"
}

response = requests.post(url, headers=headers, files=files, data=data)
result = response.json()
print(result)
```

```javascript JavaScript
const fs = require('fs');
const FormData = require('form-data');
const fetch = require('node-fetch');

const url = "https://gptproto.com/v1/images/edits";
const formData = new FormData();

formData.append("model", "gemini-2.5-flash-image-preview");
formData.append("image", fs.createReadStream("/path/to/image.png"));
formData.append("prompt", "Add a wizard hat to the cat's head");
formData.append("n", 1);
formData.append("size", "1024x1024");
formData.append("response_format", "url");

fetch(url, {
  method: "POST",
  headers: {
    "Authorization": "YOUR_API_KEY",
    ...formData.getHeaders()
  },
  body: formData
})
  .then(response => response.json())
  .then(data => console.log(data))
  .catch(error => console.error("Error:", error));
```

```go Go
package main

import (
    "bytes"
    "fmt"
    "io"
    "mime/multipart"
    "net/http"
    "os"
)

func main() {
    url := "https://gptproto.com/v1/images/edits"

    file, err := os.Open("/path/to/image.png")
    if err != nil {
        panic(err)
    }
    defer file.Close()

    body := &bytes.Buffer{}
    writer := multipart.NewWriter(body)

    part, err := writer.CreateFormFile("image", "image.png")
    if err != nil {
        panic(err)
    }
    io.Copy(part, file)

    writer.WriteField("model", "gemini-2.5-flash-image-preview")
    writer.WriteField("prompt", "Add a wizard hat to the cat's head")
    writer.WriteField("n", "1")
    writer.WriteField("size", "1024x1024")
    writer.WriteField("response_format", "url")
    writer.Close()

    req, _ := http.NewRequest("POST", url, body)
    req.Header.Set("Authorization", "YOUR_API_KEY")
    req.Header.Set("Content-Type", writer.FormDataContentType())

    client := &http.Client{}
    resp, err := client.Do(req)
    if err != nil {
        panic(err)
    }
    defer resp.Body.Close()

    responseBody, _ := io.ReadAll(resp.Body)
    fmt.Println(string(responseBody))
}
```
</CodeGroup>

<CodeGroup>

```bash cURL
curl -X POST "https://gptproto.com/v1/images/edits" \
  -H "Authorization: YOUR_API_KEY" \
  -F "model=gemini-2.5-flash-image-preview" \
  -F "image=@/path/to/image.png" \
  -F "mask=@/path/to/mask.png" \
  -F "prompt=Fill the masked area with a starry night sky" \
  -F "n=1" \
  -F "size=1024x1024"
```

```python Python
import requests

url = "https://gptproto.com/v1/images/edits"
headers = {
    "Authorization": "YOUR_API_KEY"
}

files = {
    "image": open("/path/to/image.png", "rb"),
    "mask": open("/path/to/mask.png", "rb")
}

data = {
    "model": "gemini-2.5-flash-image-preview",
    "prompt": "Fill the masked area with a starry night sky",
    "n": 1,
    "size": "1024x1024"
}

response = requests.post(url, headers=headers, files=files, data=data)
result = response.json()
print(result)
```
</CodeGroup>

<CodeGroup>

```json 401 - Invalid signature
{
  "error": {
    "message": "Invalid signature",
    "type": "401"
  }
}
```

```json 403 - Invalid Token
{
  "error": {
    "message": "Invalid Token",
    "type": "403"
  }
}
```

```json 403 - Insufficient balance
{
  "error": {
    "message": "Insufficient balance",
    "type": "403"
  }
}
```

```json 500 - Internal server error
{
  "error": {
    "message": "Internal server error",
    "type": "500"
  }
}
```

```json 503 - Content policy violation
{
  "error": {
    "message": "Input may not meet the guidelines. Please adjust and try again.",
    "type": "503"
  }
}
```
</CodeGroup>

## Parameters

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `model` | string | ✅ Yes | `gemini-2.5-flash-image-preview` | The model to use for image editing. Use `gemini-2.5-flash-image-preview` or `gemini-2.5-flash-image` for production workflows. |
| `image` | file | ✅ Yes | - | The image file to edit. Must be a valid PNG file, less than 4MB, and square for full OpenAI compatibility. Supported formats: PNG, JPEG, WebP. |
| `prompt` | string | ✅ Yes | `Add a wizard hat to the subject` | A text description of the desired image modifications. Maximum length: 1000 characters (OpenAI standard) / 32,000 characters (extended support). |
| `mask` | file | ❌ No | - | An optional mask image (PNG format) with the same dimensions as the input image. Transparent areas (alpha = 0) indicate regions to be edited. Must be less than 4MB. |
| `n` | integer | ❌ No | `1` | Number of edited images to generate. Accepts values between 1-10. |
| `size` | string | ❌ No | `1024x1024` | The size of the generated images. Must be one of `256x256`, `512x512`, or `1024x1024` for OpenAI compatibility. |
| `response_format` | string | ❌ No | `url` | The format in which the generated images are returned. Must be one of `url` or `b64_json`. URLs are valid for 60 minutes. |
| `user` | string | ❌ No | - | A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. |




### Basic Image Edit



### Image Edit with Mask

When you want to edit only specific regions of an image, use a mask:



## OpenAI SDK Compatibility

This endpoint is fully compatible with the OpenAI Python SDK:

```python
from openai import OpenAI

client = OpenAI(
    api_key="YOUR_API_KEY",
    base_url="https://gptproto.com/v1"
)

response = client.images.edit(
    model="gemini-2.5-flash-image-preview",
    image=open("/path/to/image.png", "rb"),
    prompt="Add a wizard hat to the cat's head",
    n=1,
    size="1024x1024"
)

image_url = response.data[0].url
print(image_url)
```

## Use Cases

**Object Addition/Removal**
- Add accessories or items to photos (e.g., "add a wizard hat to the cat")
- Remove unwanted objects or people from scenes
- Clean up backgrounds by removing clutter or stains

**Style & Color Modifications**
- Apply artistic styles to existing photos
- Adjust color grading and tone
- Colorize black and white photographs
- Change lighting or mood of images

**Background Editing**
- Blur or replace backgrounds
- Change environments while keeping subjects
- Adjust depth of field effects

**Creative Transformations**
- Alter poses or positions of subjects
- Merge elements from multiple images
- Create variations of product photos
- Generate consistent character representations across scenes





